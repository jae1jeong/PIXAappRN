import React from 'react'
import { StyleSheet, Text, View } from 'react-native'
import { FlatList } from 'react-native-gesture-handler'

const ResurantList = () => {
    const resutrants = {
        name: "fist"
    }
    return (
        <View>
            <FlatList data={}/>
        </View>
    )
}

export default ResurantList

const styles = StyleSheet.create({})
